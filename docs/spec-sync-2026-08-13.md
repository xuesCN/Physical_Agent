# SPEC 与代码一致性核查（2026-08-13）

核查基准：当前工作树（分支 `codex/agent-core-refactor`，HEAD `820664f`，含未提交的 003 在飞改动）。
事实优先级：可执行代码 > 通过中的测试 > 类型/契约定义 > SPEC 文档。未修改任何代码或测试。

## 扫描范围

**SPEC 文档（可修改）**——原任务模板中的路径占位符为空，按 `AGENTS.md` 的指认取：

- `docs/SPEC.zh-CN.md`
- `docs/specs/001-architecture-simplification/spec.md`
- `docs/specs/002-structured-output-hardening/spec.md`
- `docs/specs/003-safety-policy-channel-hardening/spec.md`

**实现代码（只读参考）**：`physical_agent/`、`tests/`、`frontend/src/`、`car_agent/`、`scripts/`、`.github/workflows/ci.yml`、`pyproject.toml`。

---

## 已改动（1 处）

### 1. `docs/specs/001-architecture-simplification/spec.md:64` —— `--planner` 枚举值不全

- **原内容**：调试 override 使用 `physical-agent chat --planner auto|llm|rule_based`；
- **新内容**：调试 override 使用 `physical-agent chat --planner auto|llm|rule_based|tool_loop|openai_tool_loop`；
- **依据**：
  - `physical_agent/cli.py:250-254` —— `--planner` help 原文为 `Chat brain: auto, llm, rule_based, tool_loop, or openai_tool_loop.`，默认值 `"auto"`。
  - `physical_agent/agent/chat_runtime.py:1229` —— `mode == "auto"` 分支；`:1235` —— `mode in {"tool_loop", "openai_tool_loop", "openai-tool-loop"}` 映射为 `tool_loop` 模式。
  - `physical_agent/agent/planner_factory.py:22-25` —— `rule_based/rules/offline` 与 `llm/openai/openai_compatible/openai-compatible` 两组别名。
- **补充说明**：该 help 文案自 `8261e93`（2026-06-29）起即为五值，早于本 spec 落笔日期 2026-07-17，属于文档自始不全而非代码后来扩张。
- **未一并改动的相邻表述**：同句「Dashboard chat 固定 `auto`」经核实正确——`frontend/` 的 chat 请求不发送 `planner` 字段，`physical_agent/api/server.py:1819-1825` 的 `_api_chat_planner()` 将缺省与未知值一律归一为 `auto`。`tool_loop`/`openai_tool_loop` 仅 CLI 可达，API 侧会被归一掉，因此本次只扩 CLI 那一处枚举。
- **注意**：001 是已收口的历史条目（状态行：R0-R8.1 已完成）。本次只订正其中一处对**当前 CLI** 的事实陈述，未触碰任何决策记录、理由段或验收条目。若你的约定是「已收口 spec 目录一律不再改动」，请回滚此处并改记入待决。

---

## 待决（2 处）

### 1. A1.1b / 002 的状态标记：`🟡` 与「收尾中」的关闭条件似已满足

- **位置**：
  - `docs/SPEC.zh-CN.md:91`（§4 矩阵 `A1.1b` 行）：「…专项 `143 passed, 1 warning`，Python full `555 passed, 1 warning`；**待 commit/push 后转 ✅**。」
  - `docs/specs/002-structured-output-hardening/spec.md:3`：「状态：收尾中」
  - `docs/specs/002-structured-output-hardening/spec.md:38`：「commit/push 完成后关闭本规格。」
- **冲突是什么**：两处都把状态推进条件明确写成「commit/push 完成」。该条件在仓库中已可验证地满足——`git log` 中存在 `51c92d0 Harden LLM structured output handling`，且 `git branch -r --contains 51c92d0` 返回 `fork/codex/agent-core-refactor`，即已推送到远端。按字面条件，两处状态都已过时。
- **两种可能**：
  - (a) 纯粹的状态更新遗漏：应把 `🟡` 改为 `✅`、002 spec 状态改为「已关闭」。
  - (b) 关闭条件在你的实际流程里不止 commit/push：`AGENTS.md`「收工时」要求全量测试通过 + 更新 REFACTORING §1/§2 + SPEC 矩阵，`SPEC.zh-CN.md:4` 的维护规则也写明「状态以验收测试通过为准」。我没有运行 `pytest`，也未核对 REFACTORING 是否已补入该轮记录，因此无法确认整套收工仪式已完成。
- **需要你确认什么**：这两处是否只差一次状态翻牌。若是，我可以只改这两行状态标记；若还差 REFACTORING 落账或需要我先跑一次全量 `pytest` 取证，请指明。**我没有自行翻牌**——状态标记不在你列出的「事实性内容」清单内，且翻牌意味着代替你宣告本轮收工。

### 2. 003 §7 的 `car_agent/SAFETY.template.md` 尚未被 git 跟踪

- **位置**：`docs/specs/003-safety-policy-channel-hardening/spec.md:86`：「新增 tracked `car_agent/SAFETY.template.md`。」
- **冲突是什么**：文件在工作树中存在，但 `git ls-files car_agent/` 只返回 `README.md`、`README.zh-CN.md`、`driver.py`、`physical-agent.yaml`、`physical_driver.yaml`——`SAFETY.template.md` 处于未跟踪状态。同 spec §8 的交付门禁要求「clean-checkout 测试必须证明模板可交付」，clean checkout 目前拿不到该文件。
- **两种可能**：
  - (a) 文档没问题，是实现还差一次 `git add`（003 在 `SPEC.zh-CN.md:85` 中的状态本就是 🚧 执行中，未跟踪属于在飞的正常中间态）。
  - (b) 文档需要改（例如模板最终不打算 tracked）。
- **需要你确认什么**：这属于 (a) 的可能性很高，也就是**代码侧待补 `git add`，而非文档过时**——按规则我不改文档、也不动仓库跟踪状态。请确认是否只需在 003 收口时补上跟踪。003 spec 的其余条款我已逐条对过代码，全部相符（见下方核查记录）。

---

## 核查记录：已验证相符、无需改动的部分

保留此节是为了让"只改了 1 处"可被复核，而不是靠信任。

### `docs/SPEC.zh-CN.md`

| SPEC 位置 | 声明 | 代码依据 | 结论 |
| --- | --- | --- | --- |
| §0.1:11 | `driver.execute` 只在 watch 调用栈 | 全 `physical_agent/` 仅 `watch/runtime.py:363` 一处真实调用；`agent/driver_coder.py:206` 为提示字符串 | ✓ |
| §0.1:11 | `test_safety_boundaries` 强制 | `tests/test_safety_boundaries.py` 存在，且被 `ci.yml:48` 纳入 push 阻塞 smoke | ✓ |
| §0.3:23 | 校验清单「当前 10 项」 | `watch/safety.py` 去重后恰 10 个 check code：`action_id.unique`/`dependencies.completed`/`robot.known`/`robot.ready`/`capability.known`/`autonomy.allowed`/`approval.satisfied`/`params.schema`/`constraints.satisfied`/`timeout.allowed` | ✓ |
| §1:29 | `ChatPlan.agent_output` 为 Draft 机器通道 | `protocol/schemas.py:160-166`；同类中已无 `actions` 字段 | ✓ |
| §1:41 | 每个 Action 唯一 mandatory watch-owned Gate | `protocol/agent_output.py:149-163` 强制 exactly-one + id 校验；`safety_gate_task_id()` 于 `:260` | ✓ |
| §1:41 | advisory `SafetyIntent` | `protocol/actions.py:30,113`；`application/plan_compiler.py:293-298` 明确 advisory 且不得掩盖 | ✓ |
| §1:41 | `SafetyGateTask` 命名 | 该词是代码自有词汇，见 `agent/context_builder.py:635,660,671,687,718` 的 LLM prompt 文案 | ✓ 非失效标识符，未改 |
| §2 F1:51 | reply 不再承载 `action-draft` 机器协议 | 全仓 `physical_agent/` grep `action-draft`/`action_draft` 零命中 | ✓ |
| §2 F1:51 | `requires_approval` 由后端按 robot/capability 计算 | `state/sqlite.py:2149-2166`、`application/plan_compiler.py:273-285`、`watch/safety.py:196-205` | ✓ |
| §2 F3:53 | `ContextBudget` + 四路 purpose | `agent/context_builder.py:20`（`ContextPurpose = Literal["reply","proposal","planner","tool_loop"]`）、`:44` `class ContextBudget` | ✓ |
| §2 F3:53 | memory 按 importance 排序注入 | `agent/context_builder.py:270,292-294` | ✓ |
| §4 B6:79 | 当前版本不再提供迁移命令 | `cli.py` 无 `migrate-md-to-sqlite`；`LegacyMarkdownWorkspaceReader` 零命中 | ✓ |
| §4 B6:79 | legacy fail-closed 检测保留 | `state/factory.py` 显式 `backend == "markdown"` → `RETIRED_MARKDOWN_BACKEND_GUIDANCE` | ✓ |
| §4 B6:79 | `9072b4e` 历史救援 checkout | `git cat-file -t 9072b4e9fb…` → `commit` | ✓ |
| §4 F4:86 | AgentRuntime 等 `expectation_check` | `agent/runtime.py:147`、`application/output_projection.py:37` | ✓ |
| §4 VNext-0:87 | robot 默认 hardware | `protocol/schemas.py:70`、`config.py:86` 均为 `Literal["simulation","hardware"] = "hardware"` | ✓ |
| §4 VNext-2b:90 | `append_pending_actions()` 单事务 | `state/base.py:55`、`state/sqlite.py:358`（`BEGIN IMMEDIATE`） | ✓ |
| §4 A1.3b:92 | SSE 新增 `thought` 事件 | `api/server.py:1120`、`agent/chat_runtime.py:390-392,620-621` | ✓ |
| §4 W2:95 | `observe_interval_ms` 未配置时等价 `tick_ms` | `config.py:52`（默认 `None`）、`watch/runtime.py:988-998` | ✓ |
| §4 W3:96 | `ReconnectPolicy` 默认关闭 | `drivers/transport/base.py:26` | ✓ |
| §4 W6.1:99 | `watch-executor` lease | `watch/runtime.py:34` `WATCH_LEASE_NAME`；`api/server.py:1611`、`api/watch_service.py:257` | ✓ |
| §4 F5.0:101 | 仅显式速度+时长包络才开 `drive_for` | `car_agent/physical_driver.yaml` 的 `dependentRequired`（`max_abs_speed` ⇄ `max_duration_ms`） | ✓ |
| §4 C6:115 | 导航收敛为 8 项 | `frontend/src/navigation.ts:3-12` `PAGE_KEYS` 恰为 chat/overview/actions/state/hardware/memory/events/settings | ✓ |
| §4 C6.1:116 | `?page=<key>`，缺省 overview | `frontend/src/navigation.ts:16-32` | ✓ |
| §4 基建:123 | push 阻塞 3 项 / PR 额外 2 项 / 3.11+3.12 矩阵手动 | `ci.yml`：`python-smoke`+`frontend`+`tui` 无 `if`；`python-full-pr` if PR；`e2e` if PR∨dispatch；`python-full` if dispatch∧full=='true'，matrix `["3.11","3.12"]` | ✓ |
| §5:127 | Python≥3.11，doctor 在 3.10 拒绝 | `pyproject.toml` `requires-python = ">=3.11"`；`doctor.py:35` `sys.version_info >= (3, 11)` | ✓ |
| 全文 | 交叉引用链接 | 逐个 stat：superpowers 四份设计/计划、`scripts/car_first_contact.py`、`frontend/src/viewmodels/`、`docs/current-architecture-audit.html`、`design/ui-redesign-spec.zh-CN.md`、`docs/CI.zh-CN.md`、`docs/tui-command-matrix.md`、`agent-architecture-vnext.zh-CN.md`、`tui/`、三个 `specs/00X/` 目录 —— 全部存在 | ✓ 无失效链接 |

### `docs/specs/001-.../spec.md`

§5 全部 8 条验收逐条复核：`physical_agent/gui/` 源码已删（仅剩未跟踪的 `__pycache__`，`git ls-files` 零命中）；`auto_step`、`_append_actions`、`draft_actions`、`LegacyMarkdownWorkspaceReader` 全仓零命中；`ChatPlan.actions` 已无；`/api/state.actions` 仍在（`api/server.py:325`）；`physical-agent gui`（`cli.py:156-175`，带 `--no-watch`）与 `api`（`:178-196`）共用 `_serve_api_app`。§2.2 的替代入口 `setup --force`（`cli.py:64`）、`setup --smoke-test`（`:67`）、`chat --show-code-result`（`:256-259`）、`doctor`、`watch`/`api --watch` 均存在。§3 层职责表与 `physical_agent/` 实际目录结构一致。仅 §2.2 一处枚举不全，已改。

### `docs/specs/003-.../spec.md`

§2 四个 Rules key 与 `state/safety_policy.py:30-33` 的 `_RULE_KEYS` 完全一致，`max_action_timeout_s` 单独走数值校验（`:36`、`:242-246`）。§3 的 `HardSafetyPolicy`（`:93`，frozen dataclass，`__post_init__` 校验 revision/rules/digest）与 `SafetyPolicySnapshot`（`:118`，含 `guidance_digest`/`identity_digest`）均已落地；`SafetyGate.__init__` 只收 typed hard policy 并 `isinstance` 拒绝（`watch/safety.py:36-46`）；`policy_digest` 仍取自 `hard.digest`（`watch/runtime.py:650,734,1266`），软层未混入。§3 末句「`read_safety()` 保持 dict 兼容，返回 metadata、rules、agent_guidance 和三类 digest」与 `safety_policy.py:128-136` 的 `to_dict()` 六键逐一对应。§4 原子写（同目录 tmp + `flush`/`fsync` + `os.replace`）见 `state/sidecars.py:170-177`；`write_safety` 保留 guidance 并 `revision + 1` 见 `:107-116`。§5 world 摘要必保字段七项与 `agent/context_builder.py:365-371,391-397` 完全一致。唯一未落地项见待决 2。

### `docs/specs/002-.../spec.md`

§2「新建封闭、strict 的 LLM contract models」与 `agent/llm_contracts.py:24-32`（`LLMContract`，`extra="forbid"`, `strict=True`）及 `:79-80` 的 `CHAT_RESPONSE_SCHEMA`/`ACTION_PLAN_SCHEMA` 相符。§3「不再把 output validation 暴露为裸 500」与 `api/server.py:1828-1836` 的 `_structured_output_api_error()`（schema 编程错误 500、其余 502）相符。仅状态行见待决 1。

---

## 跳过的文件及原因

以下文件**未**核查，均因不在我认定的 SPEC 范围内。如需纳入请指明：

| 文件 | 原因 |
| --- | --- |
| `docs/specs/00X/plan.md`、`tasks.md`（共 6 份，含 `r8-1-review-hardening-plan.md`） | 执行计划与任务清单，非规格本体 |
| `docs/PLAYBOOK.zh-CN.md`（66 KB）、`docs/REFACTORING.zh-CN.md`（176 KB） | 实现手册与历史账本。两者体量合计 240 KB，逐条对码是独立一轮的工作量，不适合夹带在本次核查里 |
| `docs/CI.zh-CN.md` | 虽被 `SPEC.zh-CN.md:123` 指为「细节以此为准」，但属流程文档。**顺手核对过，与 `ci.yml` 相符**，未改 |
| `docs/state-backends.zh-CN.md` | 同上，属参考文档。**顺手核对过**：SQLite-only、两个 sidecar 职责、`9072b4e` 救援流程、`scripts/smoke_legacy_workspace_rescue.py` 存在——均相符，未改 |
| `docs/agent-architecture-vnext.zh-CN.md` | 设计文档（§4 多行的归属列指向它）。**未核查**，若你把它算作 SPEC 请告知——它是本次范围外体量最大的一处风险 |
| `docs/architecture-101.zh-CN.md`、`ARCHITECTURE.zh-CN.md`、`docs/system-summary.zh-CN.md`、`docs/current-architecture-audit.md` | 架构说明/审计快照 |
| `docs/brief-*.zh-CN.md`（6 份）、`docs/f0-report.zh-CN.md`、`docs/prompt-*.md` | 轮次 brief 与实验报告，按 `AGENTS.md`「本轮 brief 用完即删」属临时件 |
| `docs/design/ui-redesign-spec.zh-CN.md`、`docs/superpowers/**` | 前端与 TUI 的分设计文档 |
| `README.md`、`README.zh-CN.md`、`AGENTS.md`、`CLAUDE.md`、`car_agent/README*.md` | 用户/agent 文档，非 SPEC |
| `docs/*.html`、`*.excalidraw`、`*.svg`、`docs/tui-command-matrix.md` | 构建产物或生成物 |
| `node_modules/`、`build/`、`physical_agent/dashboard/dist/`、`.venv/`、`.tmp/`、`.superpowers/` | 依赖与构建产物（`build/lib/` 下有一份 `physical_agent` 旧副本，grep 时已排除，勿当作真源） |

---

## 统计

- **已改 1 处**：`docs/specs/001-architecture-simplification/spec.md:64`（`--planner` 枚举值补全）
- **待决 2 处**：A1.1b/002 状态翻牌条件；003 §7 模板未被 git 跟踪
- **扫描 SPEC 文件 4 份**：`docs/SPEC.zh-CN.md`、`docs/specs/001|002|003/spec.md`
- **顺手核对但未改、未计入范围 2 份**：`docs/CI.zh-CN.md`、`docs/state-backends.zh-CN.md`
- **跳过**：见上表，最大的一处未覆盖风险是 `docs/agent-architecture-vnext.zh-CN.md`

## 自检

- **是否有改动缺少代码依据**：无。唯一改动锚定 `cli.py:250-254` 与 `chat_runtime.py:1229-1236`。
- **是否动过 SPEC 以外的文件**：无。仅新增本报告；未改任何代码、测试、配置或 git 跟踪状态。
- **跳过的文件是否如实列出**：是，含两份"顺手看了但不计入范围"的文件与那份最大的未覆盖风险。
- **整体结论**：`docs/SPEC.zh-CN.md` 主体（§0 安全边界、§1 管线、§2 目标、§4 矩阵中的技术断言、§5 环境）与当前代码高度一致，未发现需要删除的"代码中已不存在"段落，也未发现代码有而 SPEC 完全缺失的重要能力。该文档 2026-08-12 刚维护过，本次核查结果与此吻合。
